import 'package:flutter/material.dart';

class WorkspaceFlowCoordinator extends StatelessWidget {
  const WorkspaceFlowCoordinator({required this.child, super.key});

  final Widget child;

  @override
  Widget build(BuildContext context) => child;
}
